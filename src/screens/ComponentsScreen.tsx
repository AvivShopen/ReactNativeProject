import React from "react";
import { InterBlackText } from "../components/InterText";

type Props = {};

const ComponentsScreen = (props: Props) => {
  return <InterBlackText>This is the components screen</InterBlackText>;
};

export default ComponentsScreen;
